import 'package:flutter/material.dart';

class BooksListScreen extends StatelessWidget {
  const BooksListScreen({super.key});
  static const routeName = 'books-list-screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(),
    );
  }
}